export default function AppHeader() {
  return (
    <header className="fixed inset-x-0 top-0 text-blue-600 font-medium text-lg bg-white bg-opacity-60 border-b">
      <nav className="flex justify-between items-center py-4 px-4 max-w-7xl mx-auto backdrop-blur">
        <a href="#">CutPx</a>

        <a href="mailto:abiodunolunu@gmail.com">Contact</a>
      </nav>
    </header>
  );
}
